using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PatrolPointData2 : MonoBehaviour
{
    public AIScript.State patrolPointState = AIScript.State.WorkingComputer;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
